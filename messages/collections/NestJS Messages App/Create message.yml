info:
  name: Create message
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/messages
  body:
    type: json
    data: |-
      {
        "content": "messages after di"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
